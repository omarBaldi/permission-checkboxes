import { FC } from 'react';
import InputCheckboxProps from './dto';

const InputCheckbox: FC<InputCheckboxProps> = ({}): JSX.Element => {
  return <input type='checkbox' name='' id='' />;
};

export default InputCheckbox;
